# 3S-NOS Secure Framework — runtime dependencies

# XML / NETCONF parsing
lxml>=4.9.0
ncclient>=0.7.0

# gNMI client (used by nos_gnmi_pool to connect to nos-acl-bridge)
pygnmi>=0.8.15

# gRPC stack (pygnmi dependency)
grpcio>=1.58.0
protobuf

# Crypto / mTLS
cryptography>=3.4.0
